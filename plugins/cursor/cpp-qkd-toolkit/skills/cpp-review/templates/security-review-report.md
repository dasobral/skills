# QKD Security Engineering Review

<!--
AGENT INSTRUCTIONS
──────────────────
1. Work through ALL five security domains (S1–S5). If a domain is clean,
   write "No issues found in this domain." — never skip a domain silently.

2. For every finding:
   - Assign a severity: CRITICAL / WARNING / SUGGESTION
   - Assign a domain tag: [S1]–[S5]
   - Cite the exact file:line reference
   - Quote the defective code verbatim
   - Explain why it matters in a QKD / cryptographic context
   - Provide a corrected code snippet (mandatory for CRITICAL and WARNING)

3. CRITICAL findings must be highlighted at the top of the output, not buried.

4. Never describe a problem without showing how to fix it.

5. Apply the threat model established in SKILL.md Step 1 before reviewing.
-->

**Date**: {{ISO_DATE}}
**Scope**: {{SCOPE}}
**Files reviewed**: {{FILE_LIST}}
**Threat model**: {{THREAT_MODEL_SUMMARY — assets, trust boundaries, adversary model}}

---

## CRITICAL Findings

<!-- Agent: list ALL critical findings here first, before other sections. -->

---

## Detailed Findings

<!-- Use one block per finding, in severity order: CRITICAL → WARNING → SUGGESTION -->

<!--
### [SEVERITY] [DOMAIN] — Short title

**File**: `path/to/file.cpp:LINE`

```cpp
// Defective code — quoted verbatim
```

**What is wrong**: Precise, actionable description of the defect.

**Why it matters**: Consequence in a QKD / cryptographic context.
  (e.g., "A replay of this message allows the adversary to cause key
  re-use, which breaks the single-use invariant and may enable
  known-plaintext attacks on traffic encrypted with the reused key.")

**Fix**:

```cpp
// Corrected code
```

**Domain check**: [S1] Authentication gaps / [S2] Key material exposure /
                  [S3] Missing zeroization / [S4] Nonce-IV reuse /
                  [S5] Protocol violations

---
-->

### Domain S1 — Authentication Gaps

<!-- unauthenticated inter-component messages, missing MAC verification -->

### Domain S2 — Key Material Exposure

<!-- key bytes in logs, std::string copies of key buffers, key data in exceptions -->

### Domain S3 — Missing Zeroization

<!-- key buffers out of scope without zeroization, free() without cleanse -->

### Domain S4 — Nonce / IV Reuse

<!-- counters resetting on restart, IVs from low-entropy timestamps, hardcoded nonces -->

### Domain S5 — Protocol Violations

<!-- QBER check bypass, non-atomic key consumption, privacy amplification skipped -->

---

## Summary Table

| Domain | Issues | Highest severity |
|--------|--------|-----------------|
| S1 — Authentication Gaps | | |
| S2 — Key Material Exposure | | |
| S3 — Missing Zeroization | | |
| S4 — Nonce / IV Reuse | | |
| S5 — Protocol Violations | | |
| **TOTAL** | | |

**Overall Risk Rating**:
<!-- 🔴 HIGH (any CRITICAL) / 🟡 MEDIUM (no CRITICAL, ≥1 WARNING) / 🟢 LOW (suggestions only) / ✅ PASS -->

---

## Threat Model Verification

| Threat model requirement | Present in code? | Notes |
|--------------------------|-----------------|-------|
| All inter-component messages authenticated | | |
| Replay protection (sequence numbers / nonces) | | |
| Key material never logged | | |
| Key buffers zeroized after use | | |
| QBER threshold enforced; session abort on violation | | |
| ETSI GS QKD 014 endpoint conformance (if applicable) | | |
| Key consumption is atomic | | |

---

*Generated by the `qkd-security-engineer` skill.*
*Re-run `/qkd-security-engineer` after applying fixes.*
