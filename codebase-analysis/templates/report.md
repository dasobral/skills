# Codebase Standards & Architecture Guide

> Auto-generated by the `codebase-analysis` skill on {{ISO_DATE}}.
> Scope: {{ANALYSIS_SCOPE}}
>
> **Do not edit this file manually.** Re-run `/codebase-analysis` to refresh.
> Update only when a pattern is intentionally and permanently changed.

---

## Project Overview

<!-- 2–4 sentences: what the project does, its primary tech stack,
     and the top-level directory map. -->

---

## Architecture

### Directory Structure

<!-- Bullet list: each top-level directory and its single responsibility. -->

### Dependency Flow

<!-- Which layers / modules may import from which. Forbidden directions. -->

### Key Architectural Patterns

<!-- e.g.:
- Use repository pattern for all persistence. Services must not call the
  database directly.
- All HTTP handlers are thin; business logic lives in service classes.
- Events are the only communication mechanism between bounded contexts.
-->

---

## Code Style & Formatting

| Rule             | Value |
|------------------|-------|
| Indentation      |       |
| Quote style      |       |
| Max line length  |       |
| Semicolons       |       |
| Trailing commas  |       |
| Brace style      |       |
| Line endings     |       |
| Blank lines      |       |

Import ordering rules:

<!-- e.g. stdlib → third-party → internal; alphabetical within groups -->

---

## Naming Conventions

| Entity                   | Convention  | Example           |
|--------------------------|-------------|-------------------|
| Local variables          |             |                   |
| Constants / env vars     |             |                   |
| Functions / methods      |             |                   |
| Classes / structs        |             |                   |
| Interfaces / protocols   |             |                   |
| Type aliases / generics  |             |                   |
| Enums / enum variants    |             |                   |
| Files                    |             |                   |
| Directories / packages   |             |                   |
| Test files               |             |                   |
| Private / internal items |             |                   |

---

## Language & Framework Rules

- **Language**: <!-- name + version -->
- **Primary framework(s)**: <!-- name + version -->
- **Runtime / platform**: <!-- Node 20, Python 3.12, JVM 21… -->

### Preferred Patterns

<!-- Enumerate idioms in active use. Be specific.
- Use `async/await`; never raw Promise chains.
- Prefer `const` over `let`; never `var`.
- Use named exports; avoid default exports.
-->

### Patterns to Avoid

<!-- Explicitly forbidden or superseded approaches.
- Do not use `class` components; use function components with hooks.
- Do not use `moment.js`; use `date-fns`.
-->

### Preferred Libraries

<!-- When multiple options exist, record which one this codebase uses.
| Purpose        | Library       |
|----------------|---------------|
| HTTP client    |               |
| Date handling  |               |
| Validation     |               |
| Logging        |               |
-->

---

## State & Data Management

<!-- How state is managed, where it lives, and how data is fetched/mutated.
- Global state: …
- Server state / caching: …
- Form state: …
- Persistence / ORM: …
- DTO / schema validation: …
-->

---

## Error Handling & Logging

### Error Propagation

<!-- Exceptions / Result types / error codes? Custom base classes? -->

### Logging

<!-- Library, log levels in use, structured vs. plain text, where logs go. -->

### User-Facing Errors

<!-- HTTP status codes, error payload shape, i18n approach. -->

---

## Testing Requirements

### Structure

<!-- Co-located vs. separate `tests/` dir. File naming pattern. -->

### Test Pyramid

<!-- Unit / integration / e2e split. What each layer covers. -->

### Tooling & Conventions

<!-- Test runner, assertion library, mock/stub/spy approach.
- Use `describe` / `it` blocks; one assertion per test preferred.
- Factory functions live in `tests/factories/`; never hardcode fixtures inline.
-->

### Coverage

<!-- Thresholds enforced by CI, if any. -->

---

## Documentation & Comments

<!-- Docstring convention: all public APIs / only complex logic / none?
- Public functions must have JSDoc / docstrings with param and return types.
- Inline comments explain *why*, not *what*.
- TODOs must include a ticket reference: `// TODO(#123): …`
-->

---

## Security & Performance Conventions

<!-- Key rules enforced in this codebase.
- Validate all external input at the boundary layer; trust internal calls.
- Secrets via environment variables only; never hard-coded.
- Paginate all list endpoints; default page size 20, max 100.
-->

---

## Developer Workflow

```bash
# Install dependencies
<command>

# Start development server / run locally
<command>

# Lint
<command>

# Format
<command>

# Type-check
<command>

# Run tests
<command>

# Build for production
<command>
```

### Pre-commit & CI Gates

<!-- Hooks or CI steps that must pass before merge. -->

### Branch & Commit Conventions

<!-- Branch naming, commit message format, PR requirements. -->

---

## What NOT To Do

<!-- Anti-patterns observed in the codebase or explicitly prohibited.
Include the dominant pattern to follow instead.
- ❌ Mixed quote styles found in legacy files — always use single quotes.
- ❌ Direct DOM manipulation outside of framework lifecycle — use refs.
-->

---

*This file is managed by the `codebase-analysis` skill.
Re-run `/codebase-analysis` after significant architectural changes.*
